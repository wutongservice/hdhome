package com.borqs.se.widget3d;

import com.borqs.se.scene.SE3DScene;

public class HorizontalWallFrame extends WallFrame {

    public HorizontalWallFrame(SE3DScene scene, String name, int index) {
        super(scene, name, index);
        mWidth = 64;
        mHeight = 43;
        mImageSize = 128;
    }

}
