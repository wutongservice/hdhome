package com.borqs.se.widget3d;

import com.borqs.se.scene.SE3DScene;

public class VerticalWallFrame extends WallFrame {

    public VerticalWallFrame(SE3DScene scene, String name, int index) {
        super(scene, name, index);
        mWidth = 43;
        mHeight = 64;
        mImageSize = 128;
    }

}
